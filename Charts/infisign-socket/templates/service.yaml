apiVersion: v1
kind: Service
metadata:
  name: infisign-socket-service
  labels:
    app: {{ .Values.spec.template.metadata.labels.app }}
spec:
  type: ClusterIP
  selector: 
    app: infisign-socket
  ports:
  - port: 7000
    targetPort: 7000
