apiVersion: v1
kind: Secret
metadata:
  name: infisign-socket-secret
  labels:
    app: {{ .Values.metadata.labels.app }}
type: Opaque
data:
  SECRET_KEY: {{ .Values.env.SECRET_KEY | b64enc | quote }}
